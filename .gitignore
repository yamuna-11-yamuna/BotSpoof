# === Node / build artifacts ===
node_modules/
dist/
build/

# === Environment files (keep secrets private) ===
.env
.env.local
*.env

# ignore env files inside subfolders (backend/frontend/etc.)
*/.env
*/.env.local
*/*.env

# === Editors / OS ===
.vscode/
.DS_Store
